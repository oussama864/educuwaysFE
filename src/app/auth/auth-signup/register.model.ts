export class Registration {
  // tslint:disable-next-line:max-line-length
  constructor(public login: string, public firstName: string, public lastName: string, public email: string, public password: string, public langKey: string, public authorities: string[]) {}
}
